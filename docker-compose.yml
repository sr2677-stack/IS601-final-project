version: "3.9"
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./calculator.db
      - SECRET_KEY=change-me-in-production
    volumes:
      - ./data:/app/data